Step-by-step, extract relevant image URLs/paths from the context chain and craft a prompt that references them.

Context chain:
{{ context }}

Extract relevant image URLs/paths and craft a prompt that references them.
Return ONLY:
{
  "prompt": string,
  "images": string[]
}