Step-by-step, extract the Codex CLI command details from the context chain below.

Context chain:
{{ context }}

Example:
{
  "prompt": "Create a React component that renders a button",
  "approvalMode": "suggest",
  "flags": ["--max-diff", "200"]
}