TASK: {{ trigger }}

AVAILABLE ACTIONS:
{% for p in availablePlugins -%}
{{p.id}}: {{p.description}}
{% for ex in p.executors -%}  - {{ex.name}}: {{ex.description}}
{% endfor %}
{% endfor %}

{% if relatedMemories -%}
CONTEXT: {{ relatedMemories }}
{% endif %}

RULES:
1. Choose plugins that match the request type (web search → search plugin, chat → chat plugin)
2. Generate content BEFORE sending response (generate_text → send_response)
3. Final step must send response back via same plugin that triggered request

FORMAT: [{"pluginId": "string", "action": "string", "parameters": {}}]